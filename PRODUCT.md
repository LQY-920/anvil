# Product

## Register

product

## Users

一名程序员的一人公司（OPC）经营者。他每天长时间盯屏，把编码任务派给 AI Agent 自主执行，自己扮演"主编/掌柜"角色：派活、盯进度、验收产出。使用场景是 Windows 桌面大屏、白天为主，浏览器常开小窗或半屏。未来可能产品化给其他一人公司/小团队。

## Product Purpose

Anvil 是自建的个人 AI Agent 工作台（一期：编码任务调度）。看板派任务 → 本地 runner 用 Kimi Code CLI 自主执行 → 实时日志流 → 产出 diff 供验收 → 合入或打回返工。存在的理由：同类产品（Multica）的商业授权限制使其不能用于将来的产品化，Anvil 从零独立实现（MIT）。成功 = 用户无需离开浏览器即可完成"派活—监工—验收"闭环，且对 Agent 的行为完全可解释。

## Brand Personality

冷静、轻快、工匠。

- 冷静：界面是每天盯八小时的工作台，不是展示柜。低视觉噪音，颜色只用于表达状态，不用于装饰。
- 轻快：操作路径短、反馈即时、动效克制（100–180ms ease-out），不挡手。
- 工匠：像一件称手的铁匠工具——扎实、精确、耐用。细节工艺（对齐、间距、对比度）是品牌本身。

## Anti-references

- 花里胡哨的渐变营销风（大面积 aurora 渐变、玻璃拟态堆叠、彩色发光卡片）
- 营销页式的大标题/大数字仪表盘（hero-metric 模板）
- 过度拟人化/花哨的 AI 吉祥物风格
- 信息稀薄的"漂亮但没法干活"的 demo 界面

## Design Principles

1. **染色即"看我"**：界面默认灰阶安静，只有需要用户注意的状态（等你验收、失败、阻塞）才使用饱和色。颜色是信号，不是皮肤。
2. **Agent 是队友，权责要分明**：Agent 有名字和身份标识（提升信任），但 issue 的归属（owner）与执行者（delegate）在界面上始终可区分。
3. **可解释优先于可美观**：Agent 的每个自主行为都要落成可扫读、可回放的事件（日志流、时间线），而不是只改最终状态。
4. **折叠是信任基线**：工具输出默认折叠、长输出截断，用户主动展开；刷屏等于失信。
5. **双主题是一等公民**：浅色为主、深色完整配套，所有颜色走语义 token，对比度两主题分别验证。

## Accessibility & Inclusion

- WCAG 2.2 AA：正文 ≥4.5:1、大文字 ≥3:1、非文字 UI 组件/边框 ≥3:1；token 注释里记录关键对比度计算
- 色弱友好：红绿 diff 之外必须用 `+`/`-` 符号与图标做第二编码；状态不靠单一色相传达（图标 + 文字并存）
- 低疲劳：浅色主题为主、避免纯白大底（微暖/微灰画布）、等宽区可深色常驻
- 响应式：看板列固定宽 + 横向滚动，面板可收折；窄窗口自动降级单栏
- 尊重 `prefers-reduced-motion`
